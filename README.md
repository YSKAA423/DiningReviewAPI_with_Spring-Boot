# DiningReviewAPI_with_Spring-Boot
A simple Spring Boot project that creates a RESTful API with Spring Data JPA for data storage. It allows basic CRUD operations and uses cURL for testing. The app runs with an H2 database and leverages Lombok to keep the code clean and concise. Great for demonstrating your ability to build and test REST APIs with Spring Boot!
